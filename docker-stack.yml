services:
  cacheserver:
    image: chocolatefrappe/linux-packages-cacheserver:dev
    ports:
      - 80:80
    volumes:
      - type: volume
        source: cache
        target: /var/cache/linux-packages
    stop_grace_period: 5m
volumes:
  cache:
